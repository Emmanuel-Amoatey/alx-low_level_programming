#include <stdio.h>

/**
 * main - this is the main function
 *
 * Return: this is the return statement
 */

int main(void)
{
	char k;

	for (k = 0 ; k < 10 ; k++)
		printf("%d", k);
	printf("\n");
	return (0);
}
