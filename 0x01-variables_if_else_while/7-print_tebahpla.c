#include <stdio.h>

/**
 * main - this is the main function of the code
 *
 * Return: this returns my code success
 */

int main(void)
{
	char l;

	for (l = 'z' ; l >= 'a' ; l--)
		putchar(l);
	putchar('\n');
	return (0);
}
