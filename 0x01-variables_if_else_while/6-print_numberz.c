#include <stdio.h>

/**
 * main - ths is the main function
 *
 * Return: this the return statement
 */

int main(void)
{
	int k;

	for (k = 0 ; k < 10 ; k++)
		putchar(k + '0');
	putchar('\n');
	return (0);
}
